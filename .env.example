# Copia este archivo a `.env` y rellena los valores reales.
# NO versiones tu `.env`. Aquí no debe haber ningún secreto real.

# Cadena de conexión de PostgreSQL para Prisma.
# Formato: postgresql://USUARIO:PASSWORD@HOST:PUERTO/BASEDATOS?schema=public
DATABASE_URL=

# Token del bot de Telegram (lo da @BotFather).
TELEGRAM_BOT_TOKEN=

# API key de Anthropic (Claude).
ANTHROPIC_API_KEY=

# Opcional: modelo de Claude a usar. Por defecto: claude-haiku-4-5
# (el más barato que resuelve bien clasificar/resumir un mensaje corto).
ANTHROPIC_MODEL=

# Opcional: fusible de coste. Máximo de llamadas a la API de Anthropic por día;
# al superarlo, se cae al categorizador offline. Por defecto: 50.
MAX_MESSAGES_PER_DAY=

# Opcional: fichero donde persiste el contador del fusible de coste (debe ser
# escribible). Por defecto: .budget.json. En Docker se fija a /data/budget.json.
BUDGET_FILE=

# Opcional: nivel de log (debug|info|warn|error). Por defecto: info.
LOG_LEVEL=
